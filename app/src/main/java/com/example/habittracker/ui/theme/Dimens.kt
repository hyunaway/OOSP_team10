// 경로: com/example/habittracker/ui/theme/Dimens.kt
package com.example.habittracker.ui.theme

import androidx.compose.ui.unit.dp

// ── 간격 (Spacing) ────────────────────────────────────────
object HabitSpacing {
    val xxs  = 2.dp
    val xs   = 4.dp
    val sm   = 8.dp
    val md   = 12.dp
    val base = 16.dp
    val lg   = 20.dp
    val xl   = 24.dp
    val xxl  = 32.dp
    val xxxl = 48.dp
}

// ── 모서리 반경 (Corner Radius) ───────────────────────────
object HabitRadius {
    val xs     = 4.dp
    val sm     = 8.dp
    val md     = 12.dp
    val lg     = 16.dp
    val card   = 20.dp
    val button = 24.dp
    val full   = 50.dp
}

// ── 그림자 / 높이 (Elevation) ─────────────────────────────
object HabitElevation {
    val none   = 0.dp
    val xs     = 1.dp
    val sm     = 2.dp
    val card   = 4.dp
    val dialog = 8.dp
}

// ── 아이콘 크기 (Icon Size) ───────────────────────────────
object HabitIconSize {
    val sm   = 16.dp
    val md   = 20.dp
    val base = 24.dp
    val lg   = 32.dp
    val xl   = 48.dp
}
